#include <stdio.h>

int main() {
        int temp;
        scanf("%d",&temp);
        if(temp > 40)
        printf("High Temperature Alert!");
        else
        printf("Temperature is Normal");


    return 0;
}